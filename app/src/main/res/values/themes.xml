<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Fconline_user_kotlin" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar" tools:ignore="NewApi">true</item>
        <item name="colorAccent">@color/black</item>
    </style>

    <style name="Theme.Fconline_user_kotlin" parent="Base.Theme.Fconline_user_kotlin" />

    <!-- Custom style for BottomNavigationView -->
    <style name="AppBottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <!-- Disable ripple effect for selected item -->
        <item name="android:colorControlHighlight">@android:color/transparent</item>
        <item name="android:theme">@style/NoRippleTheme</item>
    </style>

    <!-- Custom theme to disable the ripple effect -->
    <style name="NoRippleTheme">
        <item name="android:colorControlHighlight">@android:color/transparent</item>
    </style>

</resources>